apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-kafka
  labels:
    app: {{ .Values.kafka.labels.app }}
spec:
  clusterIP: None
  selector:
    app: {{ .Values.kafka.labels.app }}
  ports:
    - name: broker
      port: 9092
    - name: controller
      port: 29092
