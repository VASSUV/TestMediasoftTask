spring:
  application.name: DeliveryService

  datasource:
    url: jdbc:postgresql://postgresql-warehouse-postgres-service.warehouse-db-ns.svc.cluster.local:5432/warehouse_db
    username: delivery_user
    password: delivery_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: delivery_service

  liquibase:
    default-schema: delivery_service

logging:
  level:
    org.springframework.boot.context: DEBUG

server:
  port: 8086
